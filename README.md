# Team Management Portal

Clean React + Vite + JSX frontend for the Team Management Portal.

Current phase: UI only. Supabase authentication, PostgreSQL, RLS, and Edge Functions are not connected yet.

Run:
npm install
npm run dev

Build:
npm run build

Next: Supabase Auth, profiles/tasks tables, RLS, TypeScript Edge Functions, Vercel deployment, and isolation testing.